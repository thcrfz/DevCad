const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src','config', 'database', 'databaseService.js'),
  'models-path': resolve(__dirname, 'src', 'controllers', 'presenter'),
  'migrations-path': resolve(__dirname, 'src', 'application','useCases', 'migrations'),
  'seeders-path': resolve(__dirname, 'src', 'application','useCases', 'seeders'),
};
